import React from "react";

const Logout: React.FC = () => {
    return (
        <div>
            <h1>Logout Page</h1>
            <p>Detailing the Logout process and securing of account</p>
        </div>
    );
};

export default Logout;